import { MSG } from "@/constants/MSG";

const Logo = () => {
    return(
        <div className="min-w-fit text-3xl font-bold">
            {MSG.LAYOUT.FOOTER.LOGO}
        </div>
    );
}

export default Logo;